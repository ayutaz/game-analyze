// Placeholder test file so that `node --test tests/js/ui` succeeds before any
// real UI tests have been authored. Once any real *.test.mjs file is added to
// this directory, this file can be safely deleted — it intentionally declares
// zero tests.
//
// Kept around solely so that `npm run test:js:ui` exits 0 on a fresh clone.
