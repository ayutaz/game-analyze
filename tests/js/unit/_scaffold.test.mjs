// Placeholder test file so that `node --test tests/js/unit` succeeds before
// any real unit tests have been authored. Once any real *.test.mjs file is
// added to this directory, this file can be safely deleted — it intentionally
// declares zero tests.
//
// Kept around solely so that `npm run test:js:unit` exits 0 on a fresh clone.
